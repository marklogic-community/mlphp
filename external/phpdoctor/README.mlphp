To generate a clean set of new docs

Update master branch

% cd $PATH_TO_MLPHP/mlphp/api/docs
% git rm -rf *
% cd $PATH_TO_MLPHP/mlphp/external/phpdoctor
% php phpdoctor.php mlphp.ini
% cd $PATH_TO_MLPHP/mlphp/api/docs
% git add .
% git commit -a -m "New docs"
% git push origin master

Update gh-pages copy 

% cd $PATH_TO_MLPHP/mlphp/api

If this is the first time,
% git clone -b gh-pages --single-branch git@github.com:marklogic/mlphp docs-ghpages

Otherwise
% git rm -rf [a-z]*

% cd $PATH_TO_MLPHP/mlphp/external/phpdoctor
% php phpdoctor.php mlphp-ghpages.ini
% cd PATH_TO_MLPHP/mlphp/api/docs-ghpages
% git commit -a -m "New docs"
% git push origin gh-pages
